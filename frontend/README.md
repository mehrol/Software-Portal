Wittybrains Frontend (React)
- Simple React app using axios and react-scripts
- Assumes backend is served at the same host (proxy /api -> backend)
Run:
npm install
npm start
Notes:
- This is a minimal UI; replace with Tailwind/Material UI for production polish.
- For development, set proxy in package.json or run both apps on same host.
